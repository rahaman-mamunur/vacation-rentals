   // var temp = [];

    // for(var room of duplicatehotes){
    //   var availability = true ; // Assume the room is available by default

    //   for(var booking of room.currentbookings){

    //     // check if the selected date range overlaps with any  existing bookings

    //     if(

    //       moment(dates[0]).isSameOrBefore(moment(booking.todate, 'DD-MM-YYYY')) &&
    //     moment(dates[1]).isSameOrAfter(moment(booking.fromdate, 'DD-MM-YYYY'))
    //     ){
    //       availability = false; // Mark as unavailable if there's an overlap
    //       break; // No need to check further bookings if one is already overlapping
    //     }
    //   }

    //   if (availability) {
    //     temp.push(room); // Add room to the list if it's available
    //   }

    //   setRooms(temp); // Update the state with the filtered list of available rooms
    // }



//     <div className="w-2/4">
//   <input
//     type="text"
//     className={`w-full px-4 py-2 border rounded-md focus:outline-none focus:border-blue-500 ${
//       !filsearch && '!border-red-500' // Apply '!border-red-500' class if filsearch is empty
//     }`}
//     placeholder="Search rooms"
//     value={filsearch}
//     onChange={filterBySearch}
//   />
// </div>




// className={`h-10 w-full px-4 py-2 border border-gray-300 rounded-md focus:outline-none focus:border-blue-500 ${
//    condition ? 'error-class' : ''
//  }`}
 